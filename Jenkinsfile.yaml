agent:
  label: my_label
  dockerImage: ubuntu:latest

stages:
  - stage: 'stage1'
    steps:
      - sh "docker --version"
      - sh "git --version"
  - stage: "Repo clone"
    steps:
      - git "https://github.com/damolatoba/simple-pull-request-job-plugin.git"
      - sh "echo 'Jenkins pipe don work'"
